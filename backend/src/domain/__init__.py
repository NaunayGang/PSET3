"""
Domain layer - Core business logic and entities.

This layer contains:
- Entities: Pure domain objects with business logic
- Enums: Value objects representing valid states
- Patterns: Design pattern abstractions (Observer, Command, State, Template Method, Factory)
- Repositories: Interfaces for data access (implemented in infrastructure)

The domain layer has NO dependencies on external frameworks or libraries.
"""
